#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct {
    float x, y;
    float r;
} Circle;

int cross_circle(Circle shape1, Circle shape2)

void description_circle(Circle shape1, Circle shape2)